# Web Framework & Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database & ORM (Async PostgreSQL + pgvector)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pgvector>=0.2.5

# Feed Ingestion & Parsing
feedparser>=6.0.11
pyyaml>=6.0.1
apscheduler>=3.10.4

# Vector Embeddings & Deduplication (L2)
sentence-transformers>=2.7.0
numpy>=1.26.0

# TypeSafe AI (Jev System One for Composite Scoring)
typesafe-sdk>=0.7.0

# LLM Summarizers (Google AI Studio & Groq)
google-genai>=0.1.0
groq>=0.9.0
httpx>=0.27.0

# Authentication (Argon2id password hashing)
argon2-cffi>=23.1.0

# Portfolio export (Excel)
openpyxl>=3.1.0

# Settings & Environment
pydantic>=2.0.0
pydantic-settings>=2.2.0